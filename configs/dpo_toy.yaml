# Toy DPO smoke run over the fixture world (CPU, tens of seconds).
preferences: tests/fixtures/preferences_replay.jsonl
base_checkpoint: runs/pretrain_toy
tokenizer_dir: runs/tokenizer_toy
adr_decision: ADR-0005-toy
n_ctx: 128
beta: 0.1
lr: 1.0e-4
num_train_epochs: 1.0
batch_size: 2
gradient_accumulation_steps: 1
min_margin: 0.0
logging_steps: 1
n_probe_prompts: 2
probe_max_new_tokens: 12
temperature: 0.9
length_alarm_threshold: 0.25
seed: 0
device: cpu
fp16: false
